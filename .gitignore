# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Raw source GLB/FBX uploads (processed into public/models/buildings) — kept local, not deployed
raw-assets/

# Raw asset sources (Blender/FBX scenes, original audio) — processed outputs live in public/, sources kept local
GLB/
Music/

# Internal build brief — kept local (public repo)
CLAUDE_CODE_EXECUTIVE_PRODUCT_PORTFOLIO.md

# Model/rig backups — kept local, not deployed
*.bak
